.ui.container
    table.ui.table
        thead
            tr
                th 
                th Profile
                // th Role(s)
                th Tags
                th Actions
        tbody
            each hub_users
                tr
                    td 
                        img.ui.tiny.image(src="{{c.url image_id width=100 height=100 gravity='face' crop='fill'}}")
                    td 
                        .ui.header 
                            |#{name} 
                            if profile.name
                                |(#{profile.name})
                        each emails
                            |#{address}
                        a(href="/account/profile/edit/#{_id}")
                            |Edit
                    // td 
                    //     if user_is_admin
                    //         .remove_admin.ui.basic.icon.tiny.button
                    //             i.remove.icon
                    //             |Admin
                    //     else
                    //         .make_admin.ui.basic.icon.tiny.button
                    //             i.plus.icon
                    //             |Admin
                    //     if user_is_member
                    //         .remove_member.ui.basic.icon.tiny.button
                    //             i.remove.icon
                    //             |Member
                    //     else
                    //         .make_member.ui.basic.icon.tiny.button
                    //             i.plus.icon
                    //             |Member
                    //     each roles
                    //         .ui.basic.label #{this}
                    td 
                        each tags
                            .ui.basic.label #{this}
                        if profile.tags
                            .ui.segment
                                |Old: 
                                each profile.tags
                                    .ui.basic.label #{this}
                                    
                    td 
                        if checked_in
                            .check_out.ui.basic.compact.small.button
                                i.red.remove.icon
                                |Check Out 
                        else
                            .check_in.ui.basic.compact.small.button
                                i.green.check.icon
                                |Check In 
                    //     .email_user.ui.compact.basic.disabled.button
                    //         i.mail.icon
                    //         |Email
                    //     .email_user.ui.compact.basic.disabled.button 
                    //         i.send.icon
                    //         |Direct Message
