.ui.card
    .image
        img.ui.fluid.image(src="{{c.url image_id height=400 width=800 background='white' crop='pad'}}")
    .content
        h2.ui.header #{name}
        each tags
            .ui.compact.tiny.button.eco_tag(class=eco_tag_class) #{this}